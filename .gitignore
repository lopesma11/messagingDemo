# Dependencies
node_modules/

# TypeScript compiled output
dist/

# Environment variables (never commit secrets!)
.env
.env.local
.env.*.local

# Docker volumes (local data, not part of the project)
docker-volumes/

# OS garbage
.DS_Store
Thumbs.db

# Editor configs (optional — some teams commit these)
.vscode/
.idea/